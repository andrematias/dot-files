let g:fzf_preview_window = []
let g:fzf_buffers_jump = 1
let g:fzf_commits_log_options = '--graph --color=always --format="%C(auto)%h%d %s %C(cyan)%C(bold)%cr"'
let g:fzf_layout = { 'window': '10new | set nonu | set norelativenumber' }
